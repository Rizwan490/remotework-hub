# macOS / editor junk
.DS_Store
._*
.vscode/
.idea/
*.swp

# Local server logs / scratch
*.log
probe_*.html

# NOTE: remote-jobs/, sitemap.xml and _redirects are GENERATED by
# tools/build.js but they ARE committed on purpose — Cloudflare Pages runs
# no build command, it just serves what is in the repo. Never ignore them.
